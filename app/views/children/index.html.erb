<div class="home page-container page-container--center">
  <%= render 'shared/navbar' %>

  <h1>Mes enfants</h1>

 <div class="children-grid">
    <% @children.each do |child| %>
      <%= link_to child.name, child_path(child), class:'btn button-children' %>
    <% end %>

</div>
  <button type="button" class="btn"><%= link_to "Ajouter un enfant", new_family_child_path(current_user.family_id), class: 'btn button-subscribe' %></button>
  <%= render 'shared/footer' %>
</div>
